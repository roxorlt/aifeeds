# Dashboard 环境变量参考
# 实际值按环境拷贝到 .env.local / .env.staging（git 忽略）

# Worker API base url（dev 留空 → 走 vite proxy；staging/prod 设具体域名）
VITE_API_BASE=

# 登录通道选择（备案前 email-only，备案后切 sms+email）
VITE_AUTH_CHANNEL=email
